<nav class="navbar navbar-expand-lg navbar-dark">
    <a class="navbar-brand" href="{{ path('home') }}"><img class="imgLogo" src="{{ asset('build/circus-logo.png') }}"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav w-100 d-flex justify-content-between">
            <div class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('spectacle_index') }}">Nos spectacles</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('act_index') }}">Nos numéros</a>
                </li>
            </div>
            <div class="navbar-nav">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_profile') }}">Mon compte</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_spectacle_index') }}">Admin spectacle</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_act_index') }}">Admin numéros</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_booking_index') }}">Admin réservation</a>
                        </li>
                    {% endif %}
                {%  else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                    </li>
                {% endif %}
            </div>
        </ul>
    </div>
</nav>

{% extends 'base.html.twig' %}

{% block title %}Act{% endblock %}

{% block body %}
    <h1>Act</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ act.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ act.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ act.description }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ act.created ? act.created|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ act.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('act_index') }}">back to list</a>

    <a href="{{ path('act_edit', {'id': act.id}) }}">edit</a>

    {{ include('act/_delete_form.html.twig') }}
{% endblock %}
